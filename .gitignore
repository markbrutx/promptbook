node_modules/
dist/
*.tsbuildinfo
.DS_Store
coverage/

# Next.js / Vercel build artifacts in apps/site
apps/*/.next/
apps/*/.vercel/
apps/*/.turbo/
apps/*/.source/
apps/*/next-env.d.ts
apps/*/public/demo/

# secrets & local env (never commit keys, e.g. OPENROUTER_API_KEY)
.env
.env.*
*.local

# logs & caches
*.log
.cache/

# sandbox for testing against real downstream projects — never committed
tmp/

# viewer→agent feedback queue (local, per prompts folder)
**/.annotations/

# internal, local-only docs
CLAUDE.md
codemaps/
.reports/
wb

.agents/skills/
.claude/
skills-lock.json
skills/skill-creator
